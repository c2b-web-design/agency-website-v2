{
  "_comment": "Enforced by .claude/hooks/chunk-scope-guard.js before every Edit/Write. See ai-system/live-work-protocol.md section 8. Absent file = no enforcement (opt-in by design).",

  "_status": "Chunk A COMPLETE at 3f93ce0. Chunk B COMPLETE at 0b99a36 (unlit-level A/B deliberately deferred to the light chunk). Now scoped to chunk C: the autofill cascade and masked reveal — the last of the three.",

  "chunk": "autofill-cascade-and-reveal",
  "active": true,

  "_files": "Paths the chunk may edit. Supports exact paths, 'dir/' prefixes, and * wildcards. live-work/ is included because CLAUDE.md rules 6 and 7 REQUIRE the Builder to write run logs, checkpoint requests and the session handoff there — the B-1 finding from the previous chunk's gate.",
  "files": [
    "components/enquiry/contact-field-inputs.tsx",
    "components/enquiry/contact-field-geometry.ts",
    "components/enquiry/contact-field-canvas.tsx",
    "components/enquiry/enquiry-opening.tsx",
    "verify/field-rim-state.mjs",
    "verify/_tmp-*.mjs",
    "project-intelligence/live-work/"
  ],

  "_chunk_c_note": "Chunk C: the autofill cascade and masked text reveal. Same file set as chunk B — the cascade drives the rim (canvas) and the reveal (inputs) from ONE progress value per box, so both sides move together. ⚠ STILL OUT OF BOUNDS: the entrance cascade's values, ordering and masking; the chunk-B unlit-level A/B, which must NOT be collapsed to a single value because it is deliberately deferred to the light chunk. ⚠ AND THE PREVIEW PROBLEM IS PART OF THIS CHUNK: hovering a Chrome autofill suggestion writes values into the real fields with no commitment, and moving away withdraws them — the cascade must not fire on a preview or it runs for text that vanishes.",

  "_tmp_probe_note": "verify/_tmp-*.mjs IS DECLARED THIS TIME. The Day 6 handoff recorded the advice, the satin-blue-field scope was written without it, and the guard duly blocked a throwaway probe on 31 July — the third block in two chunks. The Builder worked around it from the scratchpad, which is the DL-1 lesson: a control that blocks a required action shapes WHERE work happens, not WHETHER it happens. Finding F-6 of the chunk-A gate raised the same point about the chunk's own main file, which is why contact-field-inputs.tsx is in 'files' above.",

  "_protected": "Approved-foundation and canonical-governance paths the Builder must not touch. Denied unless the same path also appears in 'unlocked'.",
  "protected": [
    "app/globals.css",
    "components/enquiry/contact-field-mesh.tsx",
    "project-intelligence/decisions.md",
    "project-intelligence/ai-system/",
    "project-intelligence/reviews/",
    "project-intelligence/active-sprints/"
  ],

  "_unlocked": "Protected paths Carl has EXPLICITLY authorised for THIS chunk. Overrides 'protected'. Carl's call, not the Builder's. TWO unlocks, both 31 July 2026, both narrow — see the notes below. Neither is licence to restructure the file it names.",
  "unlocked": [
    "components/enquiry/enquiry-opening.tsx",
    "components/enquiry/contact-field-canvas.tsx"
  ],

  "_unlock_1_enquiry_opening": "Carl: 'Authorised to explicitly unlock.' Finding B-1 of the chunk-A plan-review gate: the chunk needs a two-line edit there (one import, one sibling element inside the existing contact-layer block) and the file is an approved-foundation path carrying the project's one accepted lint error. FOR THAT EDIT ONLY.",

  "_unlock_2_contact_field_canvas": "Carl: 'add.' FOR ONE WORD: `const FIELD_ENTRANCES` -> `export const FIELD_ENTRANCES`. Carl's instruction was 'All labels can arrive on screen with their respective boxes. Fade in with them' — a label fading on its own box's clock must read that box's delay and duration, and FIELD_ENTRANCE_END_MS only carries the aggregate. EXPORTED RATHER THAN COPIED because restating 3600/4100/4600/5100 in the DOM layer would be the FOURTH hard-coded copy of this timing, and the third went stale in verify/field-colour.mjs — which sampled every screenshot mid-cascade while reporting the settled state. NO VALUE, ORDERING OR MASKING BEHAVIOUR MAY CHANGE. The tuning block carries Carl's standing 'close enough is not approved' reservation and is not reopened by this unlock.",

  "_guard_worked": "The guard blocked this edit before the unlock existed, which is correct: unlocking is Carl's call, not the Builder's. The Builder stopped and asked rather than editing the scope file it had itself written an hour earlier — routing around a self-authored control would be the DL-1 pattern deliberately."
}
