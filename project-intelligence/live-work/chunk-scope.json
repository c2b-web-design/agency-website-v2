{
  "_comment": "Enforced by .claude/hooks/chunk-scope-guard.js before every Edit/Write. See ai-system/live-work-protocol.md section 8. Absent file = no enforcement (opt-in by design).",

  "_status": "ACTIVE. Chunk 2 of the Q&A answer-card rebuild — THE GLASS MATERIAL. Plan: .claude/plans/glowing-sniffing-quiche.md (revision 2). Briefing: live-work/architect-briefing-card-rebuild.md. Chunk 1 (geometry) is committed and approved by eye; its scope is superseded by this file.",

  "chunk": "q5-answer-card-glass",
  "active": true,

  "_files": "Paths the chunk may edit. Supports exact paths, 'dir/' prefixes, and * wildcards. live-work/ is included because CLAUDE.md rules 6 and 7 REQUIRE run logs, checkpoint requests and the session handoff to be written there (finding B-1). verify/_tmp-*.mjs and the chunk's OWN main files are declared per the carried-forward advice (findings F-6 / DL-1) — the advice was recorded twice and not followed twice, and the guard duly blocked a throwaway probe on 31 July.",
  "files": [
    "components/enquiry/answer-card-geometry.ts",
    "components/enquiry/answer-card-mesh.tsx",
    "components/enquiry/answer-card-canvas.tsx",
    "components/enquiry/answer-card-glass.ts",
    "verify/_tmp-*.mjs",
    "project-intelligence/live-work/"
  ],

  "_chunk_note": "THE FACE'S MATERIAL ONLY: MeshPhysicalMaterial with transmission, a locally generated PMREM environment map, and an OPAQUE test-pattern stand-in behind the card so the frost's detail-destroying threshold can be measured. ⚠ RIM AND BEVEL STAY DIAGNOSTIC GREY — colouring them in the same chunk that introduces glass would mix two variables Carl could otherwise separate, and contradicts chunk 1's own argument that grey exists so a form defect cannot hide behind a plausible colour. ⚠ EXPLICITLY OUT OF SCOPE: the real logo backdrop and its colour movement (chunk 3), the filament (chunk 4), rollout and the Next step button (chunk 5), text on the card's face (parked, shared with the contact field), and ANY edit to the five live CSS cards.",

  "_transmission_note": "⚠ THE TRANSMISSION PASS RENDERS opaqueObjects ONLY (three.module.js:18039), and the split at :8237 sends anything with transparent === true to the transparent list. So a material is excluded from what the glass can see if it is EITHER transmissive OR transparent. TWO CONSEQUENCES: (1) the stand-in must be transmission === 0 AND transparent === false — a 'stand-in opacity' rig control would silently break the effect with every assertion still green; (2) chunk 1's rim and bevel are ALREADY excluded during the entrance, because answer-card-canvas.tsx sets transparent = true at attach and holds it until the fade completes.",

  "_refraction_note": "⚠ REFRACTION IS NEAR-INERT UNDER THIS CAMERA AND thickness/ior SHIP FIXED. An orthographic camera down -z gives v = (0,0,1) everywhere, so the crown centre is at normal incidence and refract() returns pure -z. Measured at ior 1.45 / thickness 6: displacement is 0.000px at the face centre and 0.801px at chunk 1's measured 23.70deg maximum tilt. Sub-pixel everywhere. Exposing thickness and ior as rig knobs would move numbers and change nothing on screen — this project's own logged trap. What WebGL buys here is blur that responds to real surface curvature and real lighting in one lit scene, NOT visible bending; that is what Carl should be asked to judge.",

  "_standin_note": "⚠ A TEST PATTERN, NOT THE LOGO — and the swerve was forced by measurement. Revision 1 proposed the whole c2b mark behind one card at a claimed 4.24px thinnest stroke; that figure came from a SINGLE mid-line scan and is wrong. Scanning the whole mark: thinnest stroke is 14px of a 566px-tall mark (ratio 0.0247), which at the proposed 32px placement lands at 0.79px on screen. A 4px stroke needs the mark ~162px tall, 5x the face height. The chunk now ships parallel strokes at 2/4/6/8px and MEASURES the threshold, which hands chunk 3 arithmetic instead of a guess: a mark spanning the grid's 576px width gives a 7.18px stroke. Carl on the swerve: 'Setting the chunks is important, but something may arise in the process that we must swerve and be adaptable.'",

  "_protected": "Approved-foundation and canonical-governance paths the Builder must not touch. Denied unless the same path also appears in 'unlocked'. contact-field-* is listed in full: it is APPROVED and must not move when this card is tuned — the reason the mesh helpers are COPIED rather than imported.",
  "protected": [
    "app/globals.css",
    "components/enquiry/contact-field-mesh.tsx",
    "components/enquiry/contact-field-canvas.tsx",
    "components/enquiry/contact-field-geometry.ts",
    "components/enquiry/contact-field-inputs.tsx",
    "components/enquiry/contact-field-light-rig.tsx",
    "project-intelligence/decisions.md",
    "project-intelligence/ai-system/",
    "project-intelligence/reviews/",
    "project-intelligence/active-sprints/"
  ],

  "_unlocked": "Protected paths Carl has EXPLICITLY authorised for THIS chunk. Overrides 'protected'. Carl's call, not the Builder's.",
  "unlocked": [
    "components/enquiry/enquiry-opening.tsx"
  ],

  "_unlock_closed_then_reopened": "⚠ RETURNED TO EMPTY FOR CHUNK 2, THEN REOPENED FOR A REGRESSION FIX. It was unlocked on 3 August for chunk 1's canvas mount (+17 insertions / 0 deletions), then correctly closed because chunk 2 touches only the answer-card-* files. ⚠ THE GUARD THEN BLOCKED THE STUTTER FIX, WHICH IS THE CONTROL WORKING: the Builder stopped and asked rather than widening a scope it had itself written minutes earlier — the DL-1 pattern it must not repeat. Carl: 'fix the stutter', 3 August 2026.",

  "_unlock_stutter_fix": "FOR ONE ADDITIONAL CONDITION ONLY: `{qNum === 5 && <AnswerCardCanvas/>}` becomes `{qNum === 5 && canvasWarm && <AnswerCardCanvas/>}`, plus its comment. NOTHING ELSE MAY BE MODIFIED OR REMOVED. ⚠ THE REGRESSION IS THE BUILDER'S OWN, FROM CHUNK 1: the proto canvas mounted on `showExtras` and bypassed the `canvasWarm` gate that already existed to keep WebGL initialisation off the Q5 phrase wipe. MEASURED 3 August across 3/3 cold runs: a WebGL context created at +58-64ms into the 1300ms reveal — exactly the 'W' and 'h' of 'What' Carl reported — with a 1827-2138ms long task behind it. ⚠ AND THE LESSON IS THE SIBLING OF THE ONE ALREADY RECORDED: current-sprint.md logs a HARNESS that shared an assumption with its fix; this is a GUARD that protected one instance of a thing while a second instance was added past it. A guard written for one canvas does not cover the next one."
}
